<UserControl x:Class="ImpulseLive.WPF.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#2C3E50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部状态栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#34495E" Margin="20">
            <StackPanel Margin="0,0,40,0">
                <TextBlock Text="资金" FontSize="16" Foreground="#BDC3C7"/>
                <TextBlock Text="{Binding Player.Money, StringFormat='${0}'}" FontSize="24" FontWeight="Bold" Foreground="#2ECC71"/>
            </StackPanel>
            <StackPanel Margin="0,0,40,0">
                <TextBlock Text="理性值" FontSize="16" Foreground="#BDC3C7"/>
                <ProgressBar Value="{Binding Player.Rationality}" Maximum="100" Width="200" Height="20" Foreground="#3498DB">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Player.Rationality}" Value="50">
                                    <Setter Property="Foreground" Value="#F39C12"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Player.Rationality}" Value="20">
                                    <Setter Property="Foreground" Value="#E74C3C"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="欲望值" FontSize="16" Foreground="#BDC3C7"/>
                <TextBlock Text="{Binding Player.Desire}" FontSize="24" FontWeight="Bold" Foreground="#E74C3C"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="回合" FontSize="16" Foreground="#BDC3C7"/>
                <TextBlock Text="{Binding Round}" FontSize="24" FontWeight="Bold" Foreground="#ECF0F1"/>
            </StackPanel>
        </StackPanel>

        <!-- 中间商品区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 倒计时 -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="剩余时间" FontSize="24" FontWeight="Bold" Foreground="#ECF0F1"/>
                <TextBlock Text="{Binding RemainingTime}" FontSize="72" FontWeight="Bold" Foreground="#E74C3C" TextAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RemainingTime}" Value="5">
                                    <Setter Property="Foreground" Value="#F39C12"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

            <!-- 商品卡片 -->
            <Border Grid.Row="1" Background="#34495E" CornerRadius="10" Padding="30" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
                <StackPanel>
                    <TextBlock Text="{Binding CurrentProduct.Name}" FontSize="36" FontWeight="Bold" Foreground="#ECF0F1" TextAlignment="Center" Margin="0,0,0,20"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="价格" FontSize="18" Foreground="#BDC3C7" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CurrentProduct.Price, StringFormat='${0}'}" FontSize="32" FontWeight="Bold" Foreground="#E74C3C" Margin="0,0,0,15"/>
                            <TextBlock Text="已售" FontSize="18" Foreground="#BDC3C7" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CurrentProduct.SoldCount}" FontSize="24" Foreground="#ECF0F1"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="话术等级" FontSize="18" Foreground="#BDC3C7" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CurrentProduct.RhetoricLevel}" FontSize="32" FontWeight="Bold" Foreground="#F39C12" Margin="0,0,0,15"/>
                            <TextBlock Text="分期" FontSize="18" Foreground="#BDC3C7" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding CurrentProduct.HasInstallment, Converter={StaticResource BooleanToYesNoConverter}}" FontSize="24" Foreground="#ECF0F1"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 底部按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="20" HorizontalAlignment="Center">
            <Button Content="立即购买" Command="{Binding BuyCommand}" FontSize="24" Width="200" Height="60"
                    Background="#E74C3C" Foreground="White" BorderBrush="Transparent" FontWeight="Bold" Margin="0,0,20,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C0392B"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="加入购物车" Command="{Binding HesitateCommand}" FontSize="24" Width="200" Height="60"
                    Background="#F39C12" Foreground="White" BorderBrush="Transparent" FontWeight="Bold" Margin="0,0,20,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E67E22"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="不买" Command="{Binding RefuseCommand}" FontSize="24" Width="200" Height="60"
                    Background="#2ECC71" Foreground="White" BorderBrush="Transparent" FontWeight="Bold">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#27AE60"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>